# Variables de entorno para Railway
# Copia estas variables y agrégalas en Railway → Settings → Variables

# Supabase (si usas Supabase como base de datos)
NEXT_PUBLIC_SUPABASE_URL=https://tu-proyecto.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=tu_anon_key_aqui

# URL del servidor JSON (si usas servidor JSON local)
JSON_SERVER_URL=http://localhost:3001

# Puerto (Railway lo configura automáticamente)
PORT=3000

